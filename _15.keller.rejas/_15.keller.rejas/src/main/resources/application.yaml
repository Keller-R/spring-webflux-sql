spring:
  application:
    name: spring-webflux-sql

  # Configuración R2DBC para Neon PostgreSQL (Reactivo)
  r2dbc:
    url: r2dbc:postgresql://ep-orange-cloud-acpq9cq8-pooler.sa-east-1.aws.neon.tech:5432/keller_rejas_15?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_X1G2YymWDKHu
    pool:
      enabled: true
      initial-size: 5
      max-size: 20
      max-idle-time: 30m

  # Flyway para migraciones (usa JDBC)
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  # Configuración JDBC para Flyway
  datasource:
    url: jdbc:postgresql://ep-orange-cloud-acpq9cq8-pooler.sa-east-1.aws.neon.tech:5432/keller_rejas_15?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_X1G2YymWDKHu

# Server
server:
  port: 8080

# Logging
logging:
  level:
    org.springframework.data.r2dbc: DEBUG
    org.springframework.web.reactive: DEBUG
    io.r2dbc.postgresql: DEBUG
